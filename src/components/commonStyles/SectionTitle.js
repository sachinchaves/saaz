import styled from 'styled-components';

const SectionTitle = styled.Text`
  font-size: 24px;
  font-weight: 600;
  color: #00A347;
  margin-left: 10px;
  margin-bottom: 15px;
`;

export default SectionTitle;