import React, {useEffect, useState} from 'react';
import {
  SafeAreaView,
  FlatList,
  Text,
  Image,
  TouchableOpacity,
} from 'react-native';

const Tracks = ({navigation, route}) => {
  return <h1>Tracks</h1>;
};

export default Tracks;
